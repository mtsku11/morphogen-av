# Test Fixtures

This directory is reserved for small deterministic media fixtures. Avoid committing large binaries or copyrighted material.

- `render/flow_displace_cpu_golden.json` is a tiny text fixture for the CPU flow-displacement reference path. It stores carrier pixels, flow vectors, render amount, and expected float RGBA output.
